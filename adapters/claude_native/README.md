# Claude Native Adapter

v0.1 ships with Claude Code native execution only. Skills in this repo are Claude Code SKILL.md files — they run directly in the Claude Code skill system with no adapter layer required.

When v0.3+ adds multi-LLM support, this folder will contain the Claude adapter implementation that translates SKILL.md bodies into Claude API calls. For v0.1, no code is needed here.
